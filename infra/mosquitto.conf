# Mosquitto configuration — allow anonymous connections for the prototype
# In production you would use TLS + username/password authentication.
listener 1883
allow_anonymous true
persistence false
log_type all
